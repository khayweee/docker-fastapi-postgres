# FastAPI with Docker Stack
- Postgres Database

# Starting the service
- docker-compose up


# Check database
check db running
``` docker ps  ```
``` docker-compose exec db psql -h localhost -U postgres --dbname=postgres ```